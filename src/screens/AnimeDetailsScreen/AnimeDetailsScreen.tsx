import { StyleSheet, Text, View } from "react-native";
import React from "react";

interface Props {}

const AnimeDetailsScreen = (props: Props) => {
  return (
    <View>
      <Text>AnimeDetailsScreen</Text>
    </View>
  );
};

export default AnimeDetailsScreen;

const styles = StyleSheet.create({});
