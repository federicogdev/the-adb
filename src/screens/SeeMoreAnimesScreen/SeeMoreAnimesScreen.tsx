import { StyleSheet, Text, View } from "react-native";
import React from "react";

interface Props {}

const SeeMoreAnimesScreen = (props: Props) => {
  return (
    <View>
      <Text>SeeMoreAnimesScreen</Text>
    </View>
  );
};

export default SeeMoreAnimesScreen;

const styles = StyleSheet.create({});
